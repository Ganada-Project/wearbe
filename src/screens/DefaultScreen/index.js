import React from 'react';
import PropTypes from 'prop-types';

import { useDispatch, useSelector } from 'react-redux';
import { Wrapper } from './styles';

const DefaultScreen = () => <Wrapper></Wrapper>;

export default DefaultScreen;
